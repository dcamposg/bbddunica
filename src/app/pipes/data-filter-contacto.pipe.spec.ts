import { DataFilterContactoPipe } from './data-filter-contacto.pipe';

describe('DataFilterContactoPipe', () => {
  it('create an instance', () => {
    const pipe = new DataFilterContactoPipe();
    expect(pipe).toBeTruthy();
  });
});
