import { DataFilterFiscalEntitiesPipe } from './data-filter-fiscalEntities.pipe';

describe('DataFilterFiscalEntitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new DataFilterFiscalEntitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
