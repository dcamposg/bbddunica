import { DataFilterCentroPipe } from './data-filter-centro.pipe';

describe('DataFilterCentroPipe', () => {
  it('create an instance', () => {
    const pipe = new DataFilterCentroPipe();
    expect(pipe).toBeTruthy();
  });
});
